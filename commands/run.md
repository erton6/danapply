---
description: Start a DanApply session (alias for /danapply)
---

Run the DanApply session exactly as the main `/danapply` command does:
load the danapply skill if it isn't in context, then execute its
first-run / session-start ritual (profile check → onboarding if missing,
otherwise the greeting ritual from SKILL.md).

Treat any arguments as the user's first request.

$ARGUMENTS
